name: 构建iOS IPA
on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-ios:
    runs-on: macos-latest
    # 👇 关键：告诉 GitHub Actions 去子文件夹里执行命令
    defaults:
      run:
        working-directory: vue-xiuxiangame-main

    steps:
      - name: 拉取代码
        uses: actions/checkout@v4

      - name: 安装 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 安装依赖
        run: npm install --legacy-peer-deps

      - name: 构建前端
        run: npm run build

      - name: 同步 Capacitor iOS
        run: npx cap sync ios

      - name: 编译 iOS 归档
        run: |
          cd ios/App
          xcodebuild -workspace App.xcworkspace \
            -scheme App \
            -destination 'generic/platform=iOS' \
            -archivePath build/App.xcarchive \
            clean archive CODE_SIGNING_ALLOWED=NO

      - name: 导出 IPA
        run: |
          cd ios/App
          xcodebuild -exportArchive \
            -archivePath build/App.xcarchive \
            -exportPath build/IPA \
            -exportOptionsPlist ${{ github.workspace }}/ExportOptions.plist \
            CODE_SIGNING_ALLOWED=NO

      - name: 上传 IPA
        uses: actions/upload-artifact@v4
        with:
          name: iOS-IPA
          path: vue-xiuxiangame-main/ios/App/build/IPA/
